# Remaining Time · Roadmap

> 主线范围调整：酒馆内容及餐饮、演出、教学已从 `dev` 移除，保留在
> `experiment/tavern-agentic`。下文相关已交付记录与旧路径是历史依据，不代表主线当前能力或可用文件。

《余时遗物》：玩家在午夜前为 31 件遗物寻找归属，逐步影响一个数字生命。

本路线图以[原始规划与讨论：xnnehang.top #185](https://github.com/MrXnneHang/xnnehang.top/issues/185)为来源，结合后续剧情与协议讨论整理。后续实施在本仓库维护；原讨论保留为设计依据。已交付范围与后续计划分别标注，勾选以验收结果为准。

## 当前待办入口

本文件保留阶段规划与历史交付记录。当前本地世界的数据结构与恢复流程见[世界数据模型 · 2026-09-15](https://afterglow.xnnehang.top/architecture/world-model-2026-09-15)，已合入玩法见[每日更新](https://afterglow.xnnehang.top/daily-build/)。

后续实施跟踪于
[#190 · 余时遗物：后续实施阶段与待办](https://github.com/MrXnneHang/xnnehang.top/issues/190)：正文保留背景和索引，每个phase用一条独立评论维护清单、依赖和验收。新issue按剩余交付重新编号，各评论注明本路线图阶段的对应关系；本文件保留原阶段编号和已交付范围。[#185](https://github.com/MrXnneHang/xnnehang.top/issues/185)
已关闭，作为设计来源与实施历史保留。

## 26 房间地图 · 后续传送点校准

当前房间包已接通 26 个场景、54 个单向出口（27 对双向连接），传送物件与通道分别由
`art/room-portals.json` 和 `art/room-maps.json`
配置。自动检查覆盖出口往返与 NPC 占位；全地图的视觉位置和操作手感仍待逐图验收。

- [ ] 逐图校准门洞、楼梯、电梯和井口的传送物件，让阴影提示与实际触发格一致，室外入口位于门内，沿道路经过不误触。
- [ ] 调整入口宽度与可走通道，检查从左右边缘接近、贴墙和蹭角时的通行；台阶可走至预定触发深度。
- [ ] 逐对验收进入与返回落点，避免落地立即传送、NPC 挡路和人物重叠，并在新时间线与读档后分别复核。

本项作为后续地图体验调校，不阻塞当前房间包合入；验收以逐图实机记录为准。

## NPC 朝向与移动素材

当前房间 NPC 使用单朝向的待机、交谈和两组工作动作。场景播放待机，交互菜单中的预览面板用于逐组查看动作；正式工作动作由后续场景或任务触发。

- [ ] 先制作一名 NPC 的四向站立样板，上下左右各一帧，核对比例、脚点、光照及单侧配件；接入交谈时面向玩家的显示。
- [ ] 在站立样板上制作四向行走，每方向至少四帧，统一帧尺寸与脚点，检查循环接缝和落地滑步。
- [ ] 样板验收后逐角色扩充；只有形象对称的角色才考虑左右镜像。按现有风格交付 PNG 和动画 JSON。
- [ ] 将工作动作绑定工作地点或任务阶段，将行走动作绑定真实移动状态；闲聊入口接入 LLM 后播放交谈动作。

## 当前进度核对（酒馆帮工与本地自动存档）

当前交付是可游玩的酒馆沙盒：本地世界、JSON 内容、交易、座椅、随机客流、服务流程和短记录回放均已接通。正式内容包含酒馆一楼与二楼临时客房；404／共居走廊的双场景剧情是回归样例。五章、遗物归属与数字生命主线尚未交付。

这里的“完成”区分三种证据：实际试玩入口已接入、引擎／回归样例已支持、仅有设计。早期阶段的完整验收包含更大的内容或上线范围，不能因其中一项技术完成就勾选整个阶段。

| 阶段              | 当前状态                                       | 已完成范围                                                                                                                 | 主要剩余                                                                   |
| ----------------- | ---------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| 1 内存世界        | 已完成                                         | 本地运行、命令校验、原子提交、请求去重、状态检查和重置                                                                     | 后续按玩法扩充状态                                                         |
| 2 本地可玩界面    | 已完成基础范围                                 | 移动、碰撞、交谈、生命余额、独立剧情时钟、后台暂停                                                                         | 剧情时间的实际内容触发与反馈                                               |
| 5 scene/story MVP | 技术基础大部分完成，主线切片待做               | 多文件加载、引用校验、布尔条件、效果、顺序／并行任务、NPC 路线、双场景回归；酒馆美术与交互                                 | 正式双场景体验、404→厨房事故、遗物交互、完整视觉验收；小地图和树冠专项未做 |
| 3 记录与快照      | 连续本地记录与存档快照恢复完成，研究采集未完成 | 内容与规则版本、全局序号、输入／结果／状态记录、容量保护、JSON分段保存与快照直读                                           | 紧凑记录；研究同意、曝光记录、保留与删除                                   |
| 4 回放与调试      | 本地回放完成，内部研究工具未完成               | 存档内播放、前后切档、暂停、单步、1–8倍速、进度定位、内存检查点、暂停处续玩                                                | 文件检查点、长记录读取、行为节点与状态差异、授权和访问审计                 |
| 6 编辑与五章内容  | 内容合同和脚本工具已有，正式工作台及五章未做   | 手写／Agent 辅助 JSON、加载校验、素材生成与预览                                                                            | scene/story 图形编辑器、五区五章、31件遗物、93个机会、结局与尾声           |
| 7 画像与数字生命  | 未接入当前原型                                 | 已有任务事实可作后续输入基础                                                                                               | seed／探针、机会状态、评分重算、画像及数字生命表达                         |
| 8 发布与研究复现  | 未完成                                         | 可本地运行与构建                                                                                                           | 本项目用户世界隔离、授权、真实负载测试、部署运维与研究版本发布             |
| 9 玩家存档        | 浏览器自动存档已接入                           | 快照加载、从档首播放与前后切档、自动追加槽位、保存成功释放内存、刷新续玩、加载旧档清除后续、失败保留与重试、多页面冲突检查 | 用量管理、研究重玩样本策略                                                 |

### 酒馆切片已交付的细项

- [x] 像素地图、家具图层、独立碰撞、脚底深度和柜台外交谈站位。
- [x] 四向移动、碰壁转向、Shift 冲刺、连续动画、镜头跟随、H 隐藏 HUD、T 任务面板。
- [x] 绯月欢迎对话、六种商品、买卖、背包、生命秒价与库存；游戏剧情时间触发补货。
- [x] 16张圆凳的坐下／起身、左右坐姿、起身位置保留和整桌占用。
- [x] 四排8张桌的随机接待，5种客人外观，1–2人同行，随机选桌、入座和离店。
- [x] 随机2–7秒尝试来客、75%概率、调试阶段固定10秒用餐、最多同时接待8桌。
- [x] 莉奈接单→取餐→送餐→收桌，工作后回位与闲逛，普通对话、主角碰撞和客人重叠虚化。
- [x] 待服务桌数驱动1／1.5／2／2.5倍移速，每格生效、动画同步。
- [x] 三桌剩菜未清理时触发一次性帮工任务；首次收桌后领赠饮，后续每桌奖励300生命秒。清理可取消、玩家清理优先、报酬只结算一次。
- [x] 脏桌只影响本桌，其他空桌仍接客；相邻桌共用过道时按点击目标清理。
- [x] 服务进度、随机状态、背包及任务纳入记录、回放和续玩。
- [x] 累计1000个事件或录制缓冲达到容量上限时分段自动保存，索引发布成功后释放内存；设置中的存档窗口选择槽位、刷新自动续玩，加载旧档后清除后续记录。

实现依据：[餐饮能力](content/DINING_CAPABILITIES.md)、[内容加载器](prototype/content.ts)、[世界执行](prototype/world.ts)、[回放](prototype/replay.ts)、[餐饮回归](tests/engine/dining.test.ts)。

### 分段世界时钟与挂机保护

设计评估：采用连续积累、分段结算。默认30秒有效运行时间对应10游戏分钟，每累计一段才同时推进剧情时钟并扣除生命。以后改成60秒对应10分钟时，结算步长仍为10分钟；暂停不计入运行时间。初始余额120小时，物价和每桌5分钟报酬保持现有定位，临时工补贴日常消费。

- 默认流速为20倍：3秒清理折合1游戏分钟，5分钟工资的理论净收益为4分钟；走路、等桌及莉奈先清理都会降低实际收益。清理过程不再额外扣一次行动费，消耗随统一时钟结算。60秒一段时流速为10倍，3秒折合半分钟，经济参数仍需结合实际客流评估。
- 分段结算减弱逐秒倒计时感，但不降低总消耗：默认120小时在不计其他收支时约对应6小时有效运行，后台及闲置暂停不计入；普通对话期间周围人物与世界时钟继续运行，生命按段结算，玩家保持交谈状态。未结算进度必须保存，打开面板、切换流速、读档和回放都不能清空它。
- 当前本地酒馆的记录写入浏览器OPFS，不直接写服务器。按100ms基础步估算，一天约86.4万条advance，人物到达边界可能增加事件；真实容量还取决于每条完整状态大小。30秒扣时一次不会把录制自动变成30秒一条。

| 本轮范围           | 交付状态与边界                                                                                                                   |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------- |
| JSON分段时钟       | 已实现于PR #42分支，待验收；配置realSecondsPerTick、gameSecondsPerTick、idlePauseSeconds及初值                                   |
| 统一结算与持久进度 | 已实现，待验收；累计段进度保存在State.clock，到点一次推进10分钟并扣同量生命，HUD不逐秒递减                                       |
| 时间console        | 已实现，待验收；本地开发模式“设置→调试控制台”修改每段运行秒数，30/60秒快捷设置；setClockSpeed命令保留进度比例，进入存档和回放    |
| 挂机保护           | 已实现，待验收；默认60秒无键鼠输入暂停世界和新增模拟记录，输入恢复；后台及面板暂停，返回不补算离开时长。既有存档写入规则保持不变 |
| 清理与主动等待     | 本轮清理动画3秒、报酬5分钟；R等待结算包含已积累时间，按当前流速推进服务；T保留任务                                               |

本轮按MVP约定未新增或运行测试、类型检查、构建及浏览器验收；“已实现”不表示已通过体验或性能验收。

后续按下列顺序接入：

1. **游戏时间事件与区间耗时**：以JSON声明固定时长或如30–60游戏分钟的范围。启动时用可回放随机源抽取一次，保存参与者、开始时刻和截止点；读档、改速与回放不能重抽。以包含本段累计量的逻辑时间判定到期，HUD仍分段显示；console改变流速只影响现实等待长度。还需处理并行、取消、离场、休眠与完成效果幂等。该语法尚未进入加载器。
2. **餐饮计时迁移**：当前arrivalIntervalMs/eatMs等仍是模拟毫秒；逐项迁移接单、准备、用餐和清理的游戏时长，保留独立的动画毫秒，不能直接重命名现有字段。先落地一个真实的长耗时事件，再扩展到更多活动。
3. **经济与余时边界**：按一段营业内可获得的实际工作机会评估收入、食品与住宿支出；补齐不足一段余时时的结算与生命耗尽规则。分段展示本身不保证经济平衡。
4. **紧凑录制**：测量活跃与闲置场景每小时事件数、字节及回放耗时；采用关键输入/结果、周期快照和可合并的时间区间。现有advance分段影响人物行为，不能直接相加来压缩；需要先定义区间重放语义。保留加载、分支续玩及旧档兼容。
5. **容量与上线**：增加浏览器用量展示、保留策略与导出清理；未来上传或多用户部署再加服务端配额、世界休眠及保留期限。闲置暂停解决无人挂机增长，不替代活跃长局的容量治理。

### 二楼客房与睡眠（已实现，待验收）

- 酒馆柜台右侧设置两格宽木楼梯，左侧格间边界阻挡横穿，两列踏步可通行；双向portal连接二楼右下角三格长梯段，高斯阴影独立控制下行人物的明暗。
- 客房配置窗下书桌、右上角床与床头柜。首桌留下餐具后弹出请托提示并播放提示音；帮忙收一桌、领取汽水时，绯月邀请玩家暂住临时帮工的阁楼房间，后续收桌每桌另付五分钟余时。获邀后才激活“一个落脚点”，追踪上楼→查看床。
- 床提供睡8小时、睡到次日16:00。确认前展示醒来时刻、生命消耗和余额；sleep效果复用waitUntil，按当前流速处理楼下服务、营业和补货，失败原子回滚。
- 暂住免房费，睡眠消耗经过的生命时间；住宿价格及恢复效果留待实际玩法需求。新内容须从存档0开始；素材已生成及导出，最终布局与任务流程待验收。

### 下一轮细化任务（建议顺序，尚未实施）

优先交付可直接试玩的小闭环，每次功能只补对应的最小检查。下列是建议任务，不表示已经冻结新剧情或接入方案。

| 优先级／小交付  | 具体拆分                                                                                                     | 最小验收                                               |
| --------------- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------ |
| P1 玩家用餐     | 背包选择一件食品／饮料；确认使用并扣一件；提供文本或动画反馈；需要时再加入明确效果                           | 买入→坐下→使用→数量减少，重复请求不重复消费            |
| P1 一段真实剧情 | 选定一个事件及起止条件；安排2–3个NPC／物件交互；编写接受与拒绝选项；记录一个可见后果；给出完成反馈           | 新玩家可在5–10分钟内完成一个有选择、有结果的故事切片   |
| P1 酒馆外探索   | 在一楼↔二楼客房的双向portal基础上连接酒馆外相邻场景及返回入口，放入上述剧情的一步，保持角色、变量和物品状态 | 从酒馆走到外部场景并返回，剧情状态保留                 |
| P2 第一件遗物   | 建立单件遗物的持有与归属；提供两个可选归属对象；记录交付、拒绝及NPC反馈                                      | 一件遗物只能完成一次有效交付，选择结果在后续对话中可见 |
| P2 内容调参     | 根据实际编写需求开放常调参数，例如移速档位、闲逛间隔；沿用已有NPC台词配置，并补一个现有流程内的剧情触发点    | 内容作者修改JSON并刷新即可看到对应变化                 |
| P2 试玩调试入口 | 查看当前变量／任务／服务队列；提供必要的快速到达或测试情境命令；再按需要补管理能力                           | 测试同一剧情分支不用反复走完整段前置                   |
| 后续 长篇与工具 | 在首件遗物和首段剧情跑通后，扩展404→厨房事故、五章与更多遗物；重复编辑成本出现后建设图形工作台               | 每次扩量都有可玩内容和明确结束点                       |

原路线图的“404初醒→公共厨房事故”仍是主线目标。当前酒馆积累的是可复用玩法基础；接下来的事件可以作为试写切片，但不自动视为该主线已完成。正式研究画像、多用户上线和完整编辑器仍按后面的阶段要求推进。

## 目标与约束

- 第一阶段采用本地单世界、纯 in-memory 运行，先验证状态结构与执行语义；按适配成本复用现有移动、寻路和 Pixi 渲染。持久化与多用户隔离在后续阶段落实。
- 编剧 Agent 生成版本化 JSON；引擎负责受限条件、动作与确定的执行语义。
- 先完成“404 初醒 → 公共厨房事故”原型，再扩展至五章、31 件遗物与 93 个行为机会。
- 生命余额以H:MM:SS显示，当前时钟设计采用连续积累、每段统一扣时；默认30秒有效运行对应10游戏分钟，详见上面的分段世界时钟与挂机保护。交易与奖励即时结算，事件日程后续统一用游戏时间定义。
- 故事变量决定结局，行为证据用于探索性画像与数字生命表达；机会可达不等于有效样本齐全。
- 可视化调试回放供获授权的研发/研究人员使用；记录范围、保留期和删除机制受用户同意约束。

## 阶段 1 · 纯内存单世界原型

实现与验收：[PR #4 · In-memory single-world prototype](https://github.com/NevaMind-AI/remaining-time/pull/4)（已合并，实现与自动化验收完成）。

### 动机/背景

状态结构与剧情协议尚在探索，先用本地原型验证数据与行为，再决定持久化结构。

### 解决方案

使用普通 TypeScript 对象/Map 保存世界状态，以本地命令调用驱动变化，结果记录在内存事件数组中。仅提供初始化、执行命令、推进模拟、读取状态和重置所需的最小入口。第一阶段不接数据库、账号体系或容器；关闭进程后状态丢失是明确的原型边界。

### Todo

- [x] 建立固定样例世界与本地 runner，无数据库连接即可初始化、执行命令和推进模拟。
- [x] 用可序列化对象定义首批角色、位置与交互状态，按实际原型需求迭代。
- [x] 对命令执行运行时参数与前置条件校验；失败不留下部分变更，重复 requestId 在本次运行内只执行一次。
- [x] 在内存保留命令结果及状态变化；提供重置与状态检查，测试可注入时钟和随机源。
- [x] 验收：本地跑通初始化、移动/交互、状态检查与重置；非法命令、重复命令有可运行检查，全程无需数据库。

阶段1完成后的优先项是阶段5的scene/story
MVP；目前已交付本地短记录与回放，持久化恢复的后续安排见当前进度与执行顺序。
[原阶段计划](https://github.com/MrXnneHang/xnnehang.top/issues/185#issuecomment-5563732063)

## 阶段 2 · 本地可玩界面与时间交互

### 动机/背景

阶段 1 已提供纯内存世界与命令执行。下一步接入可视化交互，验证移动、对话和时间交易的基础体验。

### 解决方案

本地运行复用 App 页面、GameFrame 布局、TownViewport 地图镜头和 Character 渲染，使用 `/ai-town/`
路径。本地内存世界提供玩家与 N-07、方向移动和预设对话；交易确认位于对话面板内部。

实现与人工验收：[PR #7](https://github.com/NevaMind-AI/remaining-time/pull/7)（已合并，用户已确认浏览器验收完成）。下文保留该阶段的交付记录；当前时间规则见“分段世界时钟与挂机保护”。

生命余额内部以整数秒保存，界面显示 `H:MM:SS`（如
`7:00:00`，小时可超过 23）；只在明确交易或剧情效果中增减。剧情期限与移动模拟时间独立。当前会话状态在内存中运行，并通过JSON自动存档持久化；玩家可在设置中选择恢复点。

### Todo

- [x] 本地运行接入页面路径、布局、地图和角色渲染，启动无需后端或模型服务。
- [x] 实现 WASD/方向键移动、朝向、位置插值与镜头跟随；命令层检查地图碰撞及交互距离，E 交谈、Esc 退出，失焦清空按键。松键后完成当前一格再停止。
- [x] 实现预设对话、确认/退出、示例交易与移动锁定规则；UI 通过命令改变状态。
- [x] 实现整数秒生命余额及 `H:MM:SS` 显示、明确扣时、交易去重和透支提示；余额不随现实时间流逝减少。
- [x] 18:00–00:00 剧情时钟通过显式场景命令推进；后台暂停移动模拟，恢复时不补算挂机时长。
- [x] 增加时间格式、余额变化、移动、交互和重复交易的自动检查；人工验收步骤放在实现 PR。
- [x] 验收：完成移动 → 对话 → 确认交易；显示余额与实际扣时一致，重复请求不重复扣时，等待和切换后台不扣余额、不推进剧情期限。

[原阶段计划](https://github.com/MrXnneHang/xnnehang.top/issues/185#issuecomment-5563732248)

## 阶段 5 · scene/story 地图、场景与剧情 MVP（当前优先）

- [x] 碰壁转向、Shift 两倍冲刺、连续行走动画、H 隐藏/展示 HUD；入口下缘渐黑阴影提示出口方向。

当前试玩入口采用酒馆双区域布局、WASD 四向教学与看板娘绯月的欢迎对话。已接入像素美术图层、独立格碰撞、脚底定位与人物比例；桌椅按整组阻挡，壁挂灯笼共用墙体占地。对话、任务顺序及跨场景能力由固定回归内容覆盖。

商品与交易：已接入[六件食品饮料及绯月商店](art/tavern-goods/README.md)。story.items/shops 配置目录、秒价和初始店存；买入／卖出原子结算生命余额、背包与库存，1 打开背包，2打开遗物收藏，支持存档回放及续玩。买入不能透支，卖出按配置回收价结算；商店支持每24小时游戏剧情时间补货，时钟推进与补货共同记录；当前分段时钟与主动等待见上述本轮范围。食用效果待实现，自动存档已保存并恢复背包进度。

下一步先完成人物与柜台／桌椅比例、各通道与遮挡的内容验收，再推进玩家用餐和正式剧情。16张圆凳已转换为通用 seat 实体，支持玩家坐下／起身、起身格保留、双侧坐姿及录制回放；NPC 起始坐姿由 seatedOn 支持。PR
#31 已将正式酒馆接入随机客流与点餐服务，四排8张桌均可使用，动态客人会入座、用餐和离店，莉奈及玩家可清理餐桌。搬动椅子尚未实现。

### 动机/背景

evan-ak 的
[World Description Format v2 原文](docs/references/world-format-v2.md)是协议参考；[来源与使用规则](docs/references/README.md)说明原文范围与 MVP 差异的记录要求。实现前先阅读以准确理解来源；v2 仅供参考，不是必须遵循的合同。先从最小可玩场景辨别必要能力与额外能力，允许复用、简化或偏离其结构，不以完整兼容 v2 为验收目标。当前最重要的是让内容成员通过 JSON 配置地图/场景连接与剧情，并在共用游戏界面验证。先覆盖一条真实交互链，再扩大协议范围。

### 解决方案

先定义最小场景所需的内容合同。v2 中的全局 entities、变量、状态、inspect/scripted 交互、outcome、set/inc 和条件/章节转换作为候选能力，逐项判断是否必需；只实现闭环实际需要的部分。小规模剧情在明确更新点检查条件，保持固定顺序、原子变更及循环预算。

#### 内容分工与实体身份

- `scene.json`
  描述空间：地图/资源引用、实体放置、移动能力、anchors、碰撞区域、遮挡关系及 portal 的目标场景和到达点。
- `story.json` 编排剧情：变量、对话、任务、条件、效果及探针引用；决定门何时允许通过等剧情规则。
- 内容包入口为薄 manifest（文件清单与初始场景/anchor）；scene 内定义实体及初始放置，story 通过全局唯一实体 ID 引用，可跨场景组织剧情。引擎独立维护实体当前场景与位置。
- [x] [PR #13](https://github.com/NevaMind-AI/remaining-time/pull/13)：自动加载多 scene/story，联合校验引用与冲突；浏览器内容入口为
      `public/content/demo/`。当前聚焦 rule-based 交互与任务，Agent 对接留待后续。
- 地图 Agent 与编剧 Agent 分别编辑各自配置，整个内容包统一校验、预览。文件名和下述字段属于本项目探索方案，不视为 v2 原规范。

#### 实体能力与剧情规则

scene 中的实体声明通用交互能力及参数，例如查看、开关、通行目的地；story 声明剧情条件及交互后果，例如许可解锁、首次进入触发对话和任务推进。

当前是否可交互由引擎依据能力、状态、剧情条件和距离计算，不额外维护可失配的 interactable 布尔值。开关、通行等通用执行逻辑由引擎实现。MVP 保持 scene/story 两份职责配置；跨场景通用物件确有重复时再提取独立实体资源。

#### 图层、碰撞与遮挡

- 首版图层为 ground、world、foreground；world 层按角色脚底/物体落地点 Y 排序。
- 显示、碰撞、遮挡分别描述；位置采用瓦片单位，区域相对实体位置，资源描述明确绘制原点。
- 树冠可遮挡或淡化，树干阻挡通行；透明度不决定碰撞。先用矩形区域和整个精灵淡化验证，树冠独立视觉部件按需要扩展。
- 先转换一棵树和一扇门，移除对应旧物件瓦片的重复渲染/碰撞，不迁移整张地图。

#### MVP 美术样板与体验目标

MVP 同时验证可玩性与视觉吸引力：以 404 号舱和门外共居走廊作为首批原创双场景，玩家与 N-07 作为角色样板。参考《中国式相亲》等作品的俯视角、人物比例、色板与空间密度，制作本项目原创角色和布局。

使用 gpt-image-2 辅助概念与资产生成，由人工选定风格并在游戏中验收。先确认“404 号舱＋玩家＋N-07”概念图，再拆分资源；统一瓦片/角色尺寸、绘制原点、色板、光照与朝向。概念图不等于可交付精灵表，动画须检查帧间一致性并修整。

控制规模，优先精修一个房间、一段走廊、两个角色及少量交互物件；美术验证与 scene/story 最小闭环并行。最终交付应是可以演示、视觉一致且令人愿意探索的切片，用户确认视觉与手感后再扩量。

#### 小地图

从同一 scene 地形、实体位置和 portal 生成小地图，运行时提供玩家位置与发现状态。scene 只补充不能推导的图标/标签；story/运行时决定隐藏入口和物件是否可见。首版显示地形、玩家与已知入口，任务高亮和物件筛选后续按需加入。

#### 版本与兼容

scene 和 story 分别声明 `schema_version` 与
`content_version`，各自演进。内容通过稳定事实/动作接口访问游戏状态，后端字段重构由适配层承接。

同一主版本保持既有结构与执行语义兼容；新增可选字段须保持省略时行为不变。新增动作不代表旧引擎自动支持；加载器拒绝不支持的版本、字段和操作。字段类型/单位、默认行为或执行顺序改变按破坏性变更处理，升级主版本并明确迁移路径。有真实旧版本时再实现迁移，不提前建设通用框架。

本阶段自行探索两张场景、双向入口与一段解锁剧情；scene 提供目的地，story 决定通行条件，引擎执行切换。地图切换与章节切换分别定义，验证结论反馈给 evan-ak。

### Todo

- [ ] 阅读 v2 参考，先列出最小场景的必要能力、可复用实现和额外能力，再确定 MVP 合同；记录有意义的设计差异与扩展，不为兼容参考而增加功能。
- [ ] 自行编写一个主地图和一个室内场景的最小 JSON 样例、场景资源及双向入口 anchors；以可运行闭环探索 v2 子集和最小连接约定，记录验证后的协议边界。
- [x] 实现当前MVP加载与运行时校验：场景、实体、变量、任务引用、布尔条件及受支持的effects；未知字段与操作明确报错，支持范围记录在能力表。
- [x] 从JSON创建独立内存实体和剧情状态，接入UI、方向移动和地图碰撞；台词、价格与后果可通过内容修改。
- [ ] 打通主地图 → E 交互确认入口 → 室内 scene
      → 返回主地图，定义玩家位置、余额、共享/局部变量及实体身份在切换中的保留范围。
- [ ] 场景切换同时更换地图资源、碰撞数据、实体集合和交互上下文；清空输入及待处理移动，加载失败保留原场景，陈旧交互不可写入新场景。
- [x] 回归样例跑通N-07对话→确认支付→更新余额／许可变量→NPC移动与任务推进，检查重复确认、原子性和不可用选项。正式主线演出另行验收。
- [x] 内存记录保留命令、选择、执行结果及状态变化，可导出并逐事件校验回放。
- [ ] 生成并评审 404 号舱、玩家与 N-07 的风格概念图，确认规格后再生成其余素材。
- [ ] 制作 404 号舱和共居走廊资源、玩家/N-07 四向站姿与行走帧，以及门、床、工作台、继承柜等最少物件。
- [ ] 检查像素网格、透明边缘、帧间一致性、落地点与碰撞/遮挡；在共用游戏界面验证尺寸、排序、HUD 可读性和移动质感。
- [ ] 保存生成模型、提示词、批次与修订来源，核对引入素材的使用条件；人工评审原创性与风格一致性。
- [ ] 用户验收视觉样板及可玩演示：能辨认角色、发现交互入口，往返双场景并完成一段有反馈的剧情；达到认可的视觉与手感后再扩量。
- [ ] 明确 scene 实体的通用交互能力与 story 剧情条件/后果的结合规则，验证有能力但未解锁、超出距离和已可交互三种状态；交互提示与实际执行使用相同判定。
- [x] story 的 `move_entity`
      效果可启动 NPC 当前场景逐格路线；碰撞受阻等待，运行位置独立于 scene 初始配置。样例为 N-07 对话后的南侧绕行，浏览器验收待完成。
- [ ] 用 scene/story 分工实现两张场景和剧情解锁；校验内容包内 entity
      ID、anchor、资源与传送目标引用。
- [ ] 验证一棵树的 Y 排序、树干碰撞与树冠遮挡/淡化，清理其旧瓦片的重复显示与阻挡。
- [ ] 用同一 scene 生成小地图地形、玩家位置和已知入口，测试隐藏入口不提前暴露。
- [x] 定义scene/story版本及MVP支持范围，保留旧能力的兼容回归；新增行为的历史记录仍受实际规则版本兼容范围约束。
- [ ] 验收：修改 story 改变台词、价格和解锁条件，修改 scene 改变放置与目的地；双向切图后状态正确，非法配置/版本及加载失败不破坏当前世界，测试覆盖完整交互链。

高级 overrides、依赖图调度、agentic/guided 和 v1 迁移按实际内容需求安排。此阶段为本地受控内容验证，正式研究采集前仍需落实同意、保留期与删除规则。

[原阶段计划](https://github.com/MrXnneHang/xnnehang.top/issues/185#issuecomment-5563732862)

## 阶段 3 · 同意、事实记录与回放快照（MVP 后）

### 动机/背景

现有位置历史仅供短时平滑，消息和记忆另行写入，旧输入会定期清理。长期回放需要专门的记录链路。

### 解决方案

使用版本快照、有序事实及位置历史记录已执行结果。快照用于恢复历史观察状态；按同意范围管理短期调试数据与研究数据。

### Todo

- [ ] 确定记录字段、用途、保留期及删除策略；拒绝研究采集仍可游玩。
- [ ] 定义 session/context、事件序号、各类时钟、命令因果关联与内容版本。
- [ ] 状态和事实原子提交；覆盖引擎内变化、消息、场景切换及获准保留的 Agent 实际结果。
- [ ] 保存曝光、选项顺序和确认事实；自由文本默认本地处理，保存原文需单独同意。
- [ ] 实现有界日志、版本快照、归档校验、保留期清理和用户删除；精细轨迹到期聚合或删除。
- [ ] 验收：失败/重试不产生重复或缺失提交；记录与删除策略一致，明确删除后可回放范围。

[原阶段计划](https://github.com/MrXnneHang/xnnehang.top/issues/185#issuecomment-5563732489)

## 阶段 4 · 内部回放与调试（完整记录后）

### 动机/背景

研发和获授权研究人员需要复盘场景、定位异常和检查探针。

### 解决方案

复用渲染组件，提供只读历史数据源和可控时钟；应用记录结果，不重新调用 LLM 或执行真实写入。

### Todo

- [ ] 实现内部入口、项目/会话级授权、默认脱敏和访问审计。
- [ ] 实现快照恢复、日志分页、资源版本读取及角色/消息/场景同步显示。
- [x] 本地回放支持暂停、单步、1–8倍速和事件进度定位。
- [ ] 增加行为节点跳转和状态差异检查。
- [ ] 对缺失、过期或删除的数据明确提示，按留存精度定义场景复现范围。
- [ ] 验收：随机定位与顺序播放在同一记录时点一致，无外部副作用；未授权访问被拒绝。

[原阶段计划](https://github.com/MrXnneHang/xnnehang.top/issues/185#issuecomment-5563732659)

## 阶段 6 · 编辑工作台与五章内容

### 动机/背景

五章剧情包含 31 件遗物、93 个观测机会及更多推进节点，需要直观编辑与覆盖检查。

### 解决方案

同一 Schema 驱动 AI 草稿、图形预览/编辑、校验、沙盒试玩与人工发布。已发布内容版本不可变。

### Todo

- [ ] 实现 JSON 导入导出、节点图、搜索、字段错误定位和版本差异。
- [ ] 实现属性/连线编辑、撤销重做及按章节查看；展示探针与资源引用。
- [ ] 完成五区 anchors、遗物继承柜、承诺与关系、五章流程、结局判定及 NPC 尾声。
- [ ] 校验 31 个 seed 各有 A/B/C，章节配额为 19/18/19/18/19；验证主线兜底与拒绝路径。
- [ ] 按实际内容需要加入 agentic/guided 对话，落实意图确认、完成判定与分类失败回退。
- [ ] 验收：100+ 节点可编辑和发布；在途剧情/回放固定旧版本，实际试玩验证 60–90 分钟目标与机会可达性。

[原阶段计划](https://github.com/MrXnneHang/xnnehang.top/issues/185#issuecomment-5563733040)

## 阶段 7 · 探针重算、画像与数字生命

### 动机/背景

新增分析规则需要复用历史证据；剧情反馈与研究结论需要各自保持可解释边界。

### 解决方案

原始事实与版本化评分分离。事件管理器产生语义事实，分析层生成行为 step 和探针结果；缺失信息返回证据不足。

### Todo

- [ ] 获取并核验 seeds.json 内容及文档所列 SHA-256，建立题库、剧本、探针的版本关联。
- [ ] 实现 LOCKED 至 RESOLVED 的机会状态，并处理拒绝、技术中断、读档和有效曝光；仅有效确认产生信号。
- [ ] 保留每次尝试的事实，按版本化策略选择分析样本；支持幂等重算、断点续算与新旧结果比较。
- [ ] 评审收益、资源、角色扮演和前序选择的混杂；将三次机会视为跨情境观测，验证评分与证据强度规则。
- [ ] 实现 31 维行为摘要、情境差异、新生命表达与玩家数据查看/导出/删除；制度结局使用故事变量。
- [ ] 用留出会话验证分身选择一致性，区分真人/模型证据；研究招募前审查同意、事后说明及适用伦理要求。
- [ ] 验收：两版探针对同一历史生成可追溯结果；缺失为 null，反馈不宣称心理诊断或已验证人格测量。

[原阶段计划](https://github.com/MrXnneHang/xnnehang.top/issues/185#issuecomment-5563733213)

## 阶段 8 · 容量、发布与研究复现

### 动机/背景

独立世界需要可量化的服务成本和隔离保障，论文实验需要可复现版本。

### 解决方案

按真实游玩负载压测并决定部署粒度；记录代码、剧本、探针、模型和数据处理版本。

### Todo

- [ ] 从阶段 2 开始测量延迟；对 1/10/50 个活跃世界测试输入排队、数据库冲突、日志体积和 LLM 费用。
- [ ] 确认 P95 响应/回放定位与每用户小时成本预算，配置 NPC/LLM 配额、超时及休眠。
- [ ] 上线前接通真实认证与世界 owner；将默认世界入口改为当前用户世界。
- [ ] 完成数据库结构/索引及存量数据迁移，补齐 memory、embedding、消息、输入结果的 worldId 隔离与持久化去重。
- [ ] 对世界读写、管理、回放和导出落实服务端授权；双账号越权测试通过后才开放多用户测试。
- [ ] 根据实测确定共享实例或容器隔离，固定部署版本、TLS、秘密管理与监控。
- [ ] 完成备份恢复、账号删除、故障重试和跨用户安全验收。
- [ ] 发布可复现实验 tag/release、来源引用与 CITATION.cff；按授权范围准备数据/材料发布和论文归档。
- [ ] 总验收：独立游玩、五章剧情、93 个可达机会、续玩、内部授权回放、探针重算和目标并发全部通过。

[原阶段计划](https://github.com/MrXnneHang/xnnehang.top/issues/185#issuecomment-5563733391)

## 阶段 9 · 玩家存档与读档

### 当前实现

浏览器私有目录按段保存JSON；刷新恢复最新槽位。设置中可选择播放本档开头或加载本档末尾，回放提供上一档／下一档和自动接档。观看不改变时间线；加载或从回放位置继续游戏才清除后续记录。

### Todo

- [x] 固定世界、剧情、交易、计时器、命令去重及内容版本范围，通过存档末尾快照直接恢复。
- [x] 校验序号连续性、内容／规则绑定和逐事件状态，恢复失败保留当前世界及已有存档。
- [x] 自动保存索引发布后释放内存；空间不足保留记录，多页面revision冲突拒绝写入。
- [x] 验收：连续轮转、刷新恢复、选择中间槽位、删除后续、续玩及去重。
- [x] 持久化快照直接恢复；旧档首次补齐命令去重信息。
- [ ] 长篇性能基线与磁盘用量管理。
- [ ] 研究采集场景的权限、同意、删除与重玩样本选择策略。

[阶段讨论](https://github.com/MrXnneHang/xnnehang.top/issues/185#issuecomment-5566242704)

## 预留阶段 · 内容协议与世界连接对接（待 evan-ak 方案收敛）

### 动机/背景

编剧成员、美工与前端通过 JSON 进行内容协作，由后端引擎解释执行。evan-ak 正在推进模型与结构，本阶段先保留对接位置，排期与编号待依赖明确后确定。

### 已记录方向

- **Everything is
  Entity**：每个物体定义为 entity；对话、LLM 接入及介入程度、深层 Agent 交互由其能力决定。
- **world.json**：覆盖地图生成、剧情生成及世界/剧情连接。例如大地图与赌坊分别对应 world.json，进入赌坊时加载对应世界。
- 内容协作者以 JSON 为修改入口，后端负责承接内容协议。

### Todo

- [ ] 接收 evan-ak 收敛后的 entity/world.json 方案与内容样例。
- [ ] 与编剧、美工和前端确认 JSON 内容边界及引擎需支持的能力。
- [ ] 协议明确后再拆分实现任务，并评估与阶段 5/6、记录/回放的依赖。

当前仅记录方向，不冻结字段、模型、加载架构，也不触发现有后端改造。

讨论：[Everything is Entity](https://github.com/MrXnneHang/xnnehang.top/issues/185#issuecomment-5566301153)
·
[world.json 与世界连接](https://github.com/MrXnneHang/xnnehang.top/issues/185#issuecomment-5566301349)

## 游戏内调试管理台

本轮先提供本地开发模式的时间流速入口：设置→调试控制台，使用setClockSpeed领域命令修改运行状态，不改写JSON内容；改动随记录保存、回放。其余命令集、权限、操作历史和研究调试标记按下面的阶段目标继续实现。

### 动机/背景

管理员通过游戏内 `~` 管理台快速构造测试情境，检查剧情、状态和埋点，减少重复游玩。

### 解决方案

预定义命令复用领域命令入口、参数校验与状态规则。第一批提供状态检查和余额设置；任务命令随任务系统加入。仅在本地或获授权内部环境开放，不执行任意 JavaScript。

强制设置任务状态用于构造测试情境；恢复任务前的完整世界则依赖阶段 3/4 的记录与快照，在独立调试副本中执行。操作审计保存命令、操作者、前后状态与结果；调试标记传播到后续运行及派生数据，研究分析排除这些样本。

### Todo

- [ ] 实现 `~` 开关、可访问的替代入口、命令帮助、错误反馈与操作历史。
- [ ] 实现状态检查、余额设置与归零，调用既有领域规则；危险操作需确认。
- [ ] 阶段 5 任务系统就绪后增加任务完成/状态设置，明确触发效果及关联状态一致性。
- [ ] 实现入口权限、操作审计和调试数据标记；部署服务端时对每条命令授权。
- [ ] 阶段 3/4 就绪后接入历史时点恢复，保留原始记录并创建独立调试副本。
- [ ] 验收：合法命令产生预期结果，非法/越权操作不改变世界；调试数据可识别，历史恢复不改写原运行。

依赖：基础管理台在阶段 2 的 UI 和余额能力就绪后实施；任务命令与历史恢复分别随阶段 5、阶段 3/4 接入。具体命令语法待领域能力收敛，不阻塞当前可玩原型。

讨论：[Idea 6：游戏内调试管理台](https://github.com/MrXnneHang/xnnehang.top/issues/185#issuecomment-5566349224)

## 后续体验优化 · 移动与地图碰撞

Phase 2 已完成用户验收。以下由前端、地图和内容协议协作推进，不阻塞当前后端阶段。

- [ ] 支持八向移动，明确斜向速度及碰撞规则。
- [x] 已改善连续行走动画、碰壁朝向及冲刺速度配合；莉奈按忙碌程度变速，进一步美术精修按试玩反馈推进。
- [ ] 后续将 NPC 对话呈现为地图内气泡：角色姓名签、对白、指向说话者的尾角与继续提示；随镜头定位并处理边缘避让，独立于信息面板显隐。当前仅记录视觉方向，低优先级，不阻塞 scene/story
      MVP。
- [ ] 分离物体显示范围与碰撞范围：树冠负责遮挡，树干阻挡通行。
- [ ] 与 evan-ak 对接 entity/world.json 的显示区域、碰撞区域与遮挡关系，具体结构待协议收敛。

当前酒馆地图已将显示图层、脚底深度和格碰撞分开，家具支持导出配方中的像素占地矩形；树干／树冠的独立遮挡样例尚未接入。

[讨论与 Todo](https://github.com/MrXnneHang/xnnehang.top/issues/185#issuecomment-5567312726)

## 执行顺序

阶段编号保留用于追踪。当前实际进度已提前完成阶段3/4的本地短记录与回放子集；阶段5的技术基础已支持下一段正式内容。建议近期顺序以顶部细化清单为准：

一个玩家行为与一段剧情闭环 → 正式双场景与首件遗物 → 更多章节内容；按实际编辑成本建设工作台。完整研究记录／授权、画像及多用户发布依次按阶段3/4、7、8落实。

连续记录、浏览器自动存档和末尾快照恢复已接通；长篇性能实测及研究重玩策略按阶段3/4与9后续范围推进。

探针字段与隐私协议在正式研究采集前落实，容量基线按实际负载测量。下一份内容交付以实际试玩中的选择、后果和结束点验收，不能用技术回归样例代替正式主线体验。

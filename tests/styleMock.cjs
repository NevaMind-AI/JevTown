// Jest placeholder for side-effect CSS imports (e.g. `import './TimeHud.css'`).
// Vite handles these natively; Jest cannot parse them without a moduleNameMapper.
// CJS because Jest runs the suite as CommonJS (see jest.config.ts).
module.exports = {};
